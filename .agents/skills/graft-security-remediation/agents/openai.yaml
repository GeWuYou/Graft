interface:
  display_name: "Graft Security Remediation"
  short_description: "Inventory Graft security alerts before guarded remediation"
  default_prompt: "Use $graft-security-remediation in read-only inventory/review mode by default; enter local remediation or external write stages only when I explicitly authorize them."

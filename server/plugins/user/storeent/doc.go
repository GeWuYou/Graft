// Package storeent provides the user plugin's Ent-backed repository implementations.
//
// Ent-specific types stay inside this package so the rest of the user plugin
// continues to depend only on plugin-owned store contracts.
package storeent

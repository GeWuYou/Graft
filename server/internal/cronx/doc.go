// Package cronx contains the scheduler job registry for plugin-owned tasks.
//
// The scaffold captures job metadata and execution hooks now while deferring
// the real robfig/cron integration until the runtime grows beyond a shell.
package cronx

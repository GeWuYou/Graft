// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"graft/server/modules/auth/ent/authcredential"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// 认证凭据表（认证模块）
type AuthCredential struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 关联用户资料的稳定标识
	UserID uint64 `json:"user_id,omitempty"`
	// 密码哈希值
	PasswordHash *string `json:"-"`
	// 是否必须在下次登录后修改密码
	MustChangePassword bool `json:"must_change_password,omitempty"`
	// 最近一次修改密码时间
	PasswordChangedAt *time.Time `json:"password_changed_at,omitempty"`
	// 凭据创建时间
	CreatedAt time.Time `json:"created_at,omitempty"`
	// 凭据最近更新时间
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AuthCredential) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case authcredential.FieldMustChangePassword:
			values[i] = new(sql.NullBool)
		case authcredential.FieldID, authcredential.FieldUserID:
			values[i] = new(sql.NullInt64)
		case authcredential.FieldPasswordHash:
			values[i] = new(sql.NullString)
		case authcredential.FieldPasswordChangedAt, authcredential.FieldCreatedAt, authcredential.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AuthCredential fields.
func (_m *AuthCredential) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case authcredential.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case authcredential.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = uint64(value.Int64)
			}
		case authcredential.FieldPasswordHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field password_hash", values[i])
			} else if value.Valid {
				_m.PasswordHash = new(string)
				*_m.PasswordHash = value.String
			}
		case authcredential.FieldMustChangePassword:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field must_change_password", values[i])
			} else if value.Valid {
				_m.MustChangePassword = value.Bool
			}
		case authcredential.FieldPasswordChangedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field password_changed_at", values[i])
			} else if value.Valid {
				_m.PasswordChangedAt = new(time.Time)
				*_m.PasswordChangedAt = value.Time
			}
		case authcredential.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case authcredential.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AuthCredential.
// This includes values selected through modifiers, order, etc.
func (_m *AuthCredential) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this AuthCredential.
// Note that you need to call AuthCredential.Unwrap() before calling this method if this AuthCredential
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AuthCredential) Update() *AuthCredentialUpdateOne {
	return NewAuthCredentialClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AuthCredential entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AuthCredential) Unwrap() *AuthCredential {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AuthCredential is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AuthCredential) String() string {
	var builder strings.Builder
	builder.WriteString("AuthCredential(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	builder.WriteString("password_hash=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("must_change_password=")
	builder.WriteString(fmt.Sprintf("%v", _m.MustChangePassword))
	builder.WriteString(", ")
	if v := _m.PasswordChangedAt; v != nil {
		builder.WriteString("password_changed_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// AuthCredentials is a parsable slice of AuthCredential.
type AuthCredentials []*AuthCredential

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"log"
	"reflect"

	"graft/server/modules/auth/ent/migrate"

	"graft/server/modules/auth/ent/authcredential"
	"graft/server/modules/auth/ent/authpersonalaccesstoken"
	"graft/server/modules/auth/ent/authrefreshsession"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
)

// Client is the client that holds all ent builders.
type Client struct {
	config
	// Schema is the client for creating, migrating and dropping schema.
	Schema *migrate.Schema
	// AuthCredential is the client for interacting with the AuthCredential builders.
	AuthCredential *AuthCredentialClient
	// AuthPersonalAccessToken is the client for interacting with the AuthPersonalAccessToken builders.
	AuthPersonalAccessToken *AuthPersonalAccessTokenClient
	// AuthRefreshSession is the client for interacting with the AuthRefreshSession builders.
	AuthRefreshSession *AuthRefreshSessionClient
}

// NewClient creates a new client configured with the given options.
func NewClient(opts ...Option) *Client {
	client := &Client{config: newConfig(opts...)}
	client.init()
	return client
}

func (c *Client) init() {
	c.Schema = migrate.NewSchema(c.driver)
	c.AuthCredential = NewAuthCredentialClient(c.config)
	c.AuthPersonalAccessToken = NewAuthPersonalAccessTokenClient(c.config)
	c.AuthRefreshSession = NewAuthRefreshSessionClient(c.config)
}

type (
	// config is the configuration for the client and its builder.
	config struct {
		// driver used for executing database requests.
		driver dialect.Driver
		// debug enable a debug logging.
		debug bool
		// log used for logging on debug mode.
		log func(...any)
		// hooks to execute on mutations.
		hooks *hooks
		// interceptors to execute on queries.
		inters *inters
	}
	// Option function to configure the client.
	Option func(*config)
)

// newConfig creates a new config for the client.
func newConfig(opts ...Option) config {
	cfg := config{log: log.Println, hooks: &hooks{}, inters: &inters{}}
	cfg.options(opts...)
	return cfg
}

// options applies the options on the config object.
func (c *config) options(opts ...Option) {
	for _, opt := range opts {
		opt(c)
	}
	if c.debug {
		c.driver = dialect.Debug(c.driver, c.log)
	}
}

// Debug enables debug logging on the ent.Driver.
func Debug() Option {
	return func(c *config) {
		c.debug = true
	}
}

// Log sets the logging function for debug mode.
func Log(fn func(...any)) Option {
	return func(c *config) {
		c.log = fn
	}
}

// Driver configures the client driver.
func Driver(driver dialect.Driver) Option {
	return func(c *config) {
		c.driver = driver
	}
}

// Open opens a database/sql.DB specified by the driver name and
// the data source name, and returns a new client attached to it.
// Optional parameters can be added for configuring the client.
func Open(driverName, dataSourceName string, options ...Option) (*Client, error) {
	switch driverName {
	case dialect.MySQL, dialect.Postgres, dialect.SQLite:
		drv, err := sql.Open(driverName, dataSourceName)
		if err != nil {
			return nil, err
		}
		return NewClient(append(options, Driver(drv))...), nil
	default:
		return nil, fmt.Errorf("unsupported driver: %q", driverName)
	}
}

// ErrTxStarted is returned when trying to start a new transaction from a transactional client.
var ErrTxStarted = errors.New("ent: cannot start a transaction within a transaction")

// Tx returns a new transactional client. The provided context
// is used until the transaction is committed or rolled back.
func (c *Client) Tx(ctx context.Context) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, ErrTxStarted
	}
	tx, err := newTx(ctx, c.driver)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = tx
	return &Tx{
		ctx:                     ctx,
		config:                  cfg,
		AuthCredential:          NewAuthCredentialClient(cfg),
		AuthPersonalAccessToken: NewAuthPersonalAccessTokenClient(cfg),
		AuthRefreshSession:      NewAuthRefreshSessionClient(cfg),
	}, nil
}

// BeginTx returns a transactional client with specified options.
func (c *Client) BeginTx(ctx context.Context, opts *sql.TxOptions) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, errors.New("ent: cannot start a transaction within a transaction")
	}
	tx, err := c.driver.(interface {
		BeginTx(context.Context, *sql.TxOptions) (dialect.Tx, error)
	}).BeginTx(ctx, opts)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = &txDriver{tx: tx, drv: c.driver}
	return &Tx{
		ctx:                     ctx,
		config:                  cfg,
		AuthCredential:          NewAuthCredentialClient(cfg),
		AuthPersonalAccessToken: NewAuthPersonalAccessTokenClient(cfg),
		AuthRefreshSession:      NewAuthRefreshSessionClient(cfg),
	}, nil
}

// Debug returns a new debug-client. It's used to get verbose logging on specific operations.
//
//	client.Debug().
//		AuthCredential.
//		Query().
//		Count(ctx)
func (c *Client) Debug() *Client {
	if c.debug {
		return c
	}
	cfg := c.config
	cfg.driver = dialect.Debug(c.driver, c.log)
	client := &Client{config: cfg}
	client.init()
	return client
}

// Close closes the database connection and prevents new queries from starting.
func (c *Client) Close() error {
	return c.driver.Close()
}

// Use adds the mutation hooks to all the entity clients.
// In order to add hooks to a specific client, call: `client.Node.Use(...)`.
func (c *Client) Use(hooks ...Hook) {
	c.AuthCredential.Use(hooks...)
	c.AuthPersonalAccessToken.Use(hooks...)
	c.AuthRefreshSession.Use(hooks...)
}

// Intercept adds the query interceptors to all the entity clients.
// In order to add interceptors to a specific client, call: `client.Node.Intercept(...)`.
func (c *Client) Intercept(interceptors ...Interceptor) {
	c.AuthCredential.Intercept(interceptors...)
	c.AuthPersonalAccessToken.Intercept(interceptors...)
	c.AuthRefreshSession.Intercept(interceptors...)
}

// Mutate implements the ent.Mutator interface.
func (c *Client) Mutate(ctx context.Context, m Mutation) (Value, error) {
	switch m := m.(type) {
	case *AuthCredentialMutation:
		return c.AuthCredential.mutate(ctx, m)
	case *AuthPersonalAccessTokenMutation:
		return c.AuthPersonalAccessToken.mutate(ctx, m)
	case *AuthRefreshSessionMutation:
		return c.AuthRefreshSession.mutate(ctx, m)
	default:
		return nil, fmt.Errorf("ent: unknown mutation type %T", m)
	}
}

// AuthCredentialClient is a client for the AuthCredential schema.
type AuthCredentialClient struct {
	config
}

// NewAuthCredentialClient returns a client for the AuthCredential from the given config.
func NewAuthCredentialClient(c config) *AuthCredentialClient {
	return &AuthCredentialClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `authcredential.Hooks(f(g(h())))`.
func (c *AuthCredentialClient) Use(hooks ...Hook) {
	c.hooks.AuthCredential = append(c.hooks.AuthCredential, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `authcredential.Intercept(f(g(h())))`.
func (c *AuthCredentialClient) Intercept(interceptors ...Interceptor) {
	c.inters.AuthCredential = append(c.inters.AuthCredential, interceptors...)
}

// Create returns a builder for creating a AuthCredential entity.
func (c *AuthCredentialClient) Create() *AuthCredentialCreate {
	mutation := newAuthCredentialMutation(c.config, OpCreate)
	return &AuthCredentialCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of AuthCredential entities.
func (c *AuthCredentialClient) CreateBulk(builders ...*AuthCredentialCreate) *AuthCredentialCreateBulk {
	return &AuthCredentialCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *AuthCredentialClient) MapCreateBulk(slice any, setFunc func(*AuthCredentialCreate, int)) *AuthCredentialCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &AuthCredentialCreateBulk{err: fmt.Errorf("calling to AuthCredentialClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*AuthCredentialCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &AuthCredentialCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for AuthCredential.
func (c *AuthCredentialClient) Update() *AuthCredentialUpdate {
	mutation := newAuthCredentialMutation(c.config, OpUpdate)
	return &AuthCredentialUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *AuthCredentialClient) UpdateOne(_m *AuthCredential) *AuthCredentialUpdateOne {
	mutation := newAuthCredentialMutation(c.config, OpUpdateOne, withAuthCredential(_m))
	return &AuthCredentialUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *AuthCredentialClient) UpdateOneID(id int) *AuthCredentialUpdateOne {
	mutation := newAuthCredentialMutation(c.config, OpUpdateOne, withAuthCredentialID(id))
	return &AuthCredentialUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for AuthCredential.
func (c *AuthCredentialClient) Delete() *AuthCredentialDelete {
	mutation := newAuthCredentialMutation(c.config, OpDelete)
	return &AuthCredentialDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *AuthCredentialClient) DeleteOne(_m *AuthCredential) *AuthCredentialDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *AuthCredentialClient) DeleteOneID(id int) *AuthCredentialDeleteOne {
	builder := c.Delete().Where(authcredential.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &AuthCredentialDeleteOne{builder}
}

// Query returns a query builder for AuthCredential.
func (c *AuthCredentialClient) Query() *AuthCredentialQuery {
	return &AuthCredentialQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeAuthCredential},
		inters: c.Interceptors(),
	}
}

// Get returns a AuthCredential entity by its id.
func (c *AuthCredentialClient) Get(ctx context.Context, id int) (*AuthCredential, error) {
	return c.Query().Where(authcredential.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *AuthCredentialClient) GetX(ctx context.Context, id int) *AuthCredential {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *AuthCredentialClient) Hooks() []Hook {
	return c.hooks.AuthCredential
}

// Interceptors returns the client interceptors.
func (c *AuthCredentialClient) Interceptors() []Interceptor {
	return c.inters.AuthCredential
}

func (c *AuthCredentialClient) mutate(ctx context.Context, m *AuthCredentialMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&AuthCredentialCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&AuthCredentialUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&AuthCredentialUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&AuthCredentialDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown AuthCredential mutation op: %q", m.Op())
	}
}

// AuthPersonalAccessTokenClient is a client for the AuthPersonalAccessToken schema.
type AuthPersonalAccessTokenClient struct {
	config
}

// NewAuthPersonalAccessTokenClient returns a client for the AuthPersonalAccessToken from the given config.
func NewAuthPersonalAccessTokenClient(c config) *AuthPersonalAccessTokenClient {
	return &AuthPersonalAccessTokenClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `authpersonalaccesstoken.Hooks(f(g(h())))`.
func (c *AuthPersonalAccessTokenClient) Use(hooks ...Hook) {
	c.hooks.AuthPersonalAccessToken = append(c.hooks.AuthPersonalAccessToken, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `authpersonalaccesstoken.Intercept(f(g(h())))`.
func (c *AuthPersonalAccessTokenClient) Intercept(interceptors ...Interceptor) {
	c.inters.AuthPersonalAccessToken = append(c.inters.AuthPersonalAccessToken, interceptors...)
}

// Create returns a builder for creating a AuthPersonalAccessToken entity.
func (c *AuthPersonalAccessTokenClient) Create() *AuthPersonalAccessTokenCreate {
	mutation := newAuthPersonalAccessTokenMutation(c.config, OpCreate)
	return &AuthPersonalAccessTokenCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of AuthPersonalAccessToken entities.
func (c *AuthPersonalAccessTokenClient) CreateBulk(builders ...*AuthPersonalAccessTokenCreate) *AuthPersonalAccessTokenCreateBulk {
	return &AuthPersonalAccessTokenCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *AuthPersonalAccessTokenClient) MapCreateBulk(slice any, setFunc func(*AuthPersonalAccessTokenCreate, int)) *AuthPersonalAccessTokenCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &AuthPersonalAccessTokenCreateBulk{err: fmt.Errorf("calling to AuthPersonalAccessTokenClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*AuthPersonalAccessTokenCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &AuthPersonalAccessTokenCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for AuthPersonalAccessToken.
func (c *AuthPersonalAccessTokenClient) Update() *AuthPersonalAccessTokenUpdate {
	mutation := newAuthPersonalAccessTokenMutation(c.config, OpUpdate)
	return &AuthPersonalAccessTokenUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *AuthPersonalAccessTokenClient) UpdateOne(_m *AuthPersonalAccessToken) *AuthPersonalAccessTokenUpdateOne {
	mutation := newAuthPersonalAccessTokenMutation(c.config, OpUpdateOne, withAuthPersonalAccessToken(_m))
	return &AuthPersonalAccessTokenUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *AuthPersonalAccessTokenClient) UpdateOneID(id int) *AuthPersonalAccessTokenUpdateOne {
	mutation := newAuthPersonalAccessTokenMutation(c.config, OpUpdateOne, withAuthPersonalAccessTokenID(id))
	return &AuthPersonalAccessTokenUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for AuthPersonalAccessToken.
func (c *AuthPersonalAccessTokenClient) Delete() *AuthPersonalAccessTokenDelete {
	mutation := newAuthPersonalAccessTokenMutation(c.config, OpDelete)
	return &AuthPersonalAccessTokenDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *AuthPersonalAccessTokenClient) DeleteOne(_m *AuthPersonalAccessToken) *AuthPersonalAccessTokenDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *AuthPersonalAccessTokenClient) DeleteOneID(id int) *AuthPersonalAccessTokenDeleteOne {
	builder := c.Delete().Where(authpersonalaccesstoken.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &AuthPersonalAccessTokenDeleteOne{builder}
}

// Query returns a query builder for AuthPersonalAccessToken.
func (c *AuthPersonalAccessTokenClient) Query() *AuthPersonalAccessTokenQuery {
	return &AuthPersonalAccessTokenQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeAuthPersonalAccessToken},
		inters: c.Interceptors(),
	}
}

// Get returns a AuthPersonalAccessToken entity by its id.
func (c *AuthPersonalAccessTokenClient) Get(ctx context.Context, id int) (*AuthPersonalAccessToken, error) {
	return c.Query().Where(authpersonalaccesstoken.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *AuthPersonalAccessTokenClient) GetX(ctx context.Context, id int) *AuthPersonalAccessToken {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *AuthPersonalAccessTokenClient) Hooks() []Hook {
	return c.hooks.AuthPersonalAccessToken
}

// Interceptors returns the client interceptors.
func (c *AuthPersonalAccessTokenClient) Interceptors() []Interceptor {
	return c.inters.AuthPersonalAccessToken
}

func (c *AuthPersonalAccessTokenClient) mutate(ctx context.Context, m *AuthPersonalAccessTokenMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&AuthPersonalAccessTokenCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&AuthPersonalAccessTokenUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&AuthPersonalAccessTokenUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&AuthPersonalAccessTokenDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown AuthPersonalAccessToken mutation op: %q", m.Op())
	}
}

// AuthRefreshSessionClient is a client for the AuthRefreshSession schema.
type AuthRefreshSessionClient struct {
	config
}

// NewAuthRefreshSessionClient returns a client for the AuthRefreshSession from the given config.
func NewAuthRefreshSessionClient(c config) *AuthRefreshSessionClient {
	return &AuthRefreshSessionClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `authrefreshsession.Hooks(f(g(h())))`.
func (c *AuthRefreshSessionClient) Use(hooks ...Hook) {
	c.hooks.AuthRefreshSession = append(c.hooks.AuthRefreshSession, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `authrefreshsession.Intercept(f(g(h())))`.
func (c *AuthRefreshSessionClient) Intercept(interceptors ...Interceptor) {
	c.inters.AuthRefreshSession = append(c.inters.AuthRefreshSession, interceptors...)
}

// Create returns a builder for creating a AuthRefreshSession entity.
func (c *AuthRefreshSessionClient) Create() *AuthRefreshSessionCreate {
	mutation := newAuthRefreshSessionMutation(c.config, OpCreate)
	return &AuthRefreshSessionCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of AuthRefreshSession entities.
func (c *AuthRefreshSessionClient) CreateBulk(builders ...*AuthRefreshSessionCreate) *AuthRefreshSessionCreateBulk {
	return &AuthRefreshSessionCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *AuthRefreshSessionClient) MapCreateBulk(slice any, setFunc func(*AuthRefreshSessionCreate, int)) *AuthRefreshSessionCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &AuthRefreshSessionCreateBulk{err: fmt.Errorf("calling to AuthRefreshSessionClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*AuthRefreshSessionCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &AuthRefreshSessionCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for AuthRefreshSession.
func (c *AuthRefreshSessionClient) Update() *AuthRefreshSessionUpdate {
	mutation := newAuthRefreshSessionMutation(c.config, OpUpdate)
	return &AuthRefreshSessionUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *AuthRefreshSessionClient) UpdateOne(_m *AuthRefreshSession) *AuthRefreshSessionUpdateOne {
	mutation := newAuthRefreshSessionMutation(c.config, OpUpdateOne, withAuthRefreshSession(_m))
	return &AuthRefreshSessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *AuthRefreshSessionClient) UpdateOneID(id int) *AuthRefreshSessionUpdateOne {
	mutation := newAuthRefreshSessionMutation(c.config, OpUpdateOne, withAuthRefreshSessionID(id))
	return &AuthRefreshSessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for AuthRefreshSession.
func (c *AuthRefreshSessionClient) Delete() *AuthRefreshSessionDelete {
	mutation := newAuthRefreshSessionMutation(c.config, OpDelete)
	return &AuthRefreshSessionDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *AuthRefreshSessionClient) DeleteOne(_m *AuthRefreshSession) *AuthRefreshSessionDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *AuthRefreshSessionClient) DeleteOneID(id int) *AuthRefreshSessionDeleteOne {
	builder := c.Delete().Where(authrefreshsession.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &AuthRefreshSessionDeleteOne{builder}
}

// Query returns a query builder for AuthRefreshSession.
func (c *AuthRefreshSessionClient) Query() *AuthRefreshSessionQuery {
	return &AuthRefreshSessionQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeAuthRefreshSession},
		inters: c.Interceptors(),
	}
}

// Get returns a AuthRefreshSession entity by its id.
func (c *AuthRefreshSessionClient) Get(ctx context.Context, id int) (*AuthRefreshSession, error) {
	return c.Query().Where(authrefreshsession.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *AuthRefreshSessionClient) GetX(ctx context.Context, id int) *AuthRefreshSession {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *AuthRefreshSessionClient) Hooks() []Hook {
	return c.hooks.AuthRefreshSession
}

// Interceptors returns the client interceptors.
func (c *AuthRefreshSessionClient) Interceptors() []Interceptor {
	return c.inters.AuthRefreshSession
}

func (c *AuthRefreshSessionClient) mutate(ctx context.Context, m *AuthRefreshSessionMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&AuthRefreshSessionCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&AuthRefreshSessionUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&AuthRefreshSessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&AuthRefreshSessionDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown AuthRefreshSession mutation op: %q", m.Op())
	}
}

// hooks and interceptors per client, for fast access.
type (
	hooks struct {
		AuthCredential, AuthPersonalAccessToken, AuthRefreshSession []ent.Hook
	}
	inters struct {
		AuthCredential, AuthPersonalAccessToken, AuthRefreshSession []ent.Interceptor
	}
)

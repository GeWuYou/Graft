INSERT INTO "audit_policy_rules" ("name", "description", "source", "enabled", "priority", "effect", "match_type", "method", "path_pattern", "event_type", "risk_level", "target_type", "condition_expr") VALUES
  ('domain.container.volume.remove', 'Record dangerous Docker volume remove events.', 'DOMAIN_EVENT', true, 58, 'include', 'exact', '', '', 'ops.container.volume.remove', '', '', ''),
  ('domain.container.volume.remove.batch', 'Record dangerous Docker volume batch-remove summary events.', 'DOMAIN_EVENT', true, 59, 'include', 'exact', '', '', 'ops.container.volume.remove.batch', '', '', '')
ON CONFLICT ("name") DO UPDATE SET "description" = EXCLUDED."description", "source" = EXCLUDED."source", "enabled" = EXCLUDED."enabled", "priority" = EXCLUDED."priority", "effect" = EXCLUDED."effect", "match_type" = EXCLUDED."match_type", "method" = EXCLUDED."method", "path_pattern" = EXCLUDED."path_pattern", "event_type" = EXCLUDED."event_type", "risk_level" = EXCLUDED."risk_level", "target_type" = EXCLUDED."target_type", "condition_expr" = EXCLUDED."condition_expr", "updated_at" = NOW();

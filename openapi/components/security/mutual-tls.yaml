type: mutualTLS
description: Experimental Runtime Target Agent transport. The dedicated Agent listener accepts only TLS 1.3 mutual TLS with an active, certificate-bound Agent identity; bearer tokens and cookies are not accepted.

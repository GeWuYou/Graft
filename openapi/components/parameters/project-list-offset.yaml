name: offset
in: query
required: false
description: Optional zero-based offset for projects.
schema:
  type: integer
  minimum: 0
examples:
  firstPage:
    summary: Start from the first project
    value: 0

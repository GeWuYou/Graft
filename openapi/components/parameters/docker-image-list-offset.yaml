name: offset
in: query
required: false
description: Optional zero-based offset for Docker images.
schema:
  type: integer
  minimum: 0
examples:
  firstPage:
    summary: Start from the first Docker image
    value: 0

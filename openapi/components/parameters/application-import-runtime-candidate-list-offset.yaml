name: offset
in: query
required: false
description: Optional zero-based offset for runtime import candidates.
schema:
  type: integer
  minimum: 0
examples:
  firstPage:
    summary: Start from the first runtime import candidate
    value: 0

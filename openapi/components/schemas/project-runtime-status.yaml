type: string
enum: [running, degraded, stopped, transitioning, unknown]
x-enum-varnames:
  - ProjectRuntimeStatusRunning
  - ProjectRuntimeStatusDegraded
  - ProjectRuntimeStatusStopped
  - ProjectRuntimeStatusTransitioning
  - ProjectRuntimeStatusUnknown
description: >
  Stable aggregated project status for overview consumers. The value is derived from container-member
  runtime state and must not be treated as container-detail authority.

type: object
required: [success, code, message, traceId, data]
properties:
  success: { type: boolean }
  code: { type: string }
  message: { type: string }
  traceId: { type: string }
  data:
    type: object
    required: [items]
    properties:
      items: { type: array, maxItems: 100, items: { $ref: ./platform-network-connectivity-custom-target.yaml } }

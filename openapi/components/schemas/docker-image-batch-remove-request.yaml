type: object
required: [ids]
properties:
  ids:
    type: array
    minItems: 1
    maxItems: 100
    items:
      type: string
      minLength: 1
      maxLength: 512
  force:
    type: boolean
    default: false
    description: Force removal even when a container references an image.
additionalProperties: false

type: object
required: [snapshot_id, source_kind, content_digest, lifecycle_state]
properties:
  snapshot_id: { type: string }
  source_kind: { type: string, enum: [uploaded_archive] }
  content_digest: { type: string }
  lifecycle_state: { type: string, enum: [available, expired, purged] }
additionalProperties: false

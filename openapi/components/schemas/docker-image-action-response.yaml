type: object
required: [id, action, message_key]
properties:
  id:
    type: string
  action:
    type: string
    enum: [tag, untag, remove]
    x-enum-varnames:
      - DockerImageActionResponseActionTag
      - DockerImageActionResponseActionUntag
      - DockerImageActionResponseActionRemove
  message_key:
    type: string

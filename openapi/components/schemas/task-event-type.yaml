type: string
description: >
  Non-derivable Task lifecycle, retry, cancellation, and recovery fact. Stage lifecycle is authoritative in TaskStage
  and is not duplicated as an event.
enum: [created, cancel_requested, cancelled, retry_requested, retry_scheduled, recovery_required, recovery_resolved]

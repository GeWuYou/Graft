type: object
additionalProperties: false
required: [reason]
properties:
  reason: {type: string, minLength: 1, maxLength: 512, description: Operator-supplied revocation reason retained in the redacted audit record.}

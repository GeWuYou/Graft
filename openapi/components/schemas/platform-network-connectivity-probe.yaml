type: object
required: [kind, status, duration_ms]
properties:
  kind: { type: string }
  status: { type: string, enum: [succeeded, failed, skipped] }
  duration_ms: { type: integer, format: int64, minimum: 0 }
  http_status: { type: integer, minimum: 100, maximum: 599, nullable: true, description: HTTP response status when this probe received a response. }
  summary: { type: string }
  error_code: { type: string }
  occurred_at: { type: string, format: date-time }

type: object
required: [target_id, display_name, endpoint]
properties:
  target_id: { type: string, maxLength: 128, pattern: '^custom-[a-z0-9][a-z0-9-]{0,120}$' }
  display_name: { type: string, maxLength: 128 }
  endpoint: { type: string, format: uri, maxLength: 2048 }

type: object
description: A module explicitly registered as a consumer of the platform outbound-network policy.
required: [id, title_key]
properties:
  id:
    type: string
    maxLength: 128
  title_key:
    type: string
    maxLength: 256

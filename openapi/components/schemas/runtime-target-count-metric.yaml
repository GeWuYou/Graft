type: object
required: [available, total, active, unavailableReason]
properties:
  available: {type: boolean}
  total: {type: integer, format: int64}
  active: {type: integer, format: int64}
  unavailableReason: {type: string}

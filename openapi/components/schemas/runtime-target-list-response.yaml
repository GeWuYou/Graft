type: object
required: [items, total, limit, offset]
properties:
  items:
    type: array
    items:
      $ref: ./runtime-target.yaml
  total:
    type: integer
    format: int64
  limit:
    type: integer
  offset:
    type: integer

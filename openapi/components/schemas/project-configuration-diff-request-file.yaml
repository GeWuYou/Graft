type: object
required:
  - path
  - content
properties:
  path:
    type: string
    minLength: 1
    description: Project-root-relative file path for the draft override.
  content:
    type: string
    description: Draft file content that should be diffed before writing to disk.

type: object
required: [policy, diagnostic_targets, consumers]
properties:
  policy:
    $ref: ./platform-network-outbound-policy.yaml
  diagnostic_targets:
    type: array
    items:
      $ref: ./platform-network-diagnostic-target.yaml
  consumers:
    type: array
    description: Modules explicitly registered as consumers of the effective platform outbound-network policy.
    items:
      $ref: ./platform-network-consumer.yaml

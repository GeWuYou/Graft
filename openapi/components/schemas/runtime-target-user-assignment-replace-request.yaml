type: object
required: [user_ids, revision]
properties:
  revision:
    type: integer
    format: int64
    minimum: 1
    description: Assignment collection revision returned by the latest read.
  user_ids:
    type: array
    maxItems: 10000
    items: {type: integer, format: int64, minimum: 1}

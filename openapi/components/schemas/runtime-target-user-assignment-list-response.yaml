type: object
required: [items, revision]
properties:
  revision:
    type: integer
    format: int64
    minimum: 1
    description: Monotonic revision of the assignment collection.
  items:
    type: array
    items:
      $ref: ./runtime-target-user-assignment.yaml

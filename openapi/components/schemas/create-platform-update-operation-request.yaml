type: object
required: [target_version, confirmation]
properties:
  target_version: { type: string, maxLength: 128 }
  confirmation: { type: string, maxLength: 128, description: Exact target_version typed by the administrator. }

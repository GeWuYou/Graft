type: object
required: [success, code, message, traceId, data]
properties:
  success: { type: boolean }
  code: { type: string }
  message: { type: string }
  traceId: { type: string }
  data:
    type: object
    required: [items]
    properties:
      items: { type: array, items: { $ref: ./platform-network-connectivity-target.yaml } }

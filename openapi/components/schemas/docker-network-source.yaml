type: object
required: [kind]
properties:
  kind:
    type: string
    enum: [compose, swarm, docker, custom, unknown]
  compose_project: { type: string }
  compose_network: { type: string }
  compose_version: { type: string }
  swarm_stack: { type: string }

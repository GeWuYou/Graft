type: object
required: [items, next_after_sequence]
properties:
  items:
    type: array
    items: { $ref: ./task-log-entry.yaml }
  next_after_sequence: { type: integer, format: int64, minimum: 0 }

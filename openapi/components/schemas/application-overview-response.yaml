type: object
required:
  - application_id
  - compose_project_name
  - health
  - resources
  - services
properties:
  application_id:
    $ref: ./application-id.yaml
  compose_project_name:
    type: string
  collected_at:
    type: string
    format: date-time
    nullable: true
  health:
    $ref: ./application-overview-health-summary.yaml
  resources:
    $ref: ./application-overview-resource-summary.yaml
  services:
    type: array
    items:
      $ref: ./application-overview-service-item.yaml

get:
  tags: [project]
  summary: List private project-list saved views
  operationId: getProjectSavedViews
  parameters:
    - $ref: ../components/parameters/locale-header.yaml
    - $ref: ../components/parameters/request-id-header.yaml
  security:
    - bearerAuth: []
  responses:
    '200':
      description: Current user's saved project-list views.
      content:
        application/json:
          schema:
            $ref: ../components/schemas/enveloped-project-saved-view-list.yaml
    '401': { $ref: ../components/responses/unauthorized.yaml }
    '403': { $ref: ../components/responses/forbidden.yaml }
post:
  tags: [project]
  summary: Create a private project-list saved view
  operationId: postProjectSavedView
  parameters:
    - $ref: ../components/parameters/locale-header.yaml
    - $ref: ../components/parameters/request-id-header.yaml
  security:
    - bearerAuth: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../components/schemas/project-saved-view-request.yaml
  responses:
    '201':
      description: Saved view created.
      content:
        application/json:
          schema:
            $ref: ../components/schemas/enveloped-project-saved-view.yaml
    '400':
      description: Invalid saved-view state.
      content:
        application/json:
          schema: { $ref: ../components/schemas/error-response.yaml }
    '401': { $ref: ../components/responses/unauthorized.yaml }
    '403': { $ref: ../components/responses/forbidden.yaml }
    '409':
      description: Duplicate saved-view name.
      content:
        application/json:
          schema: { $ref: ../components/schemas/error-response.yaml }

get:
  tags: [build]
  operationId: getBuildJobs
  summary: List Build-owned job projections
  parameters:
    - $ref: ../components/parameters/locale-header.yaml
    - $ref: ../components/parameters/request-id-header.yaml
    - in: query
      name: limit
      schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
    - in: query
      name: offset
      schema: { type: integer, minimum: 0, default: 0 }
  security:
    - bearerAuth: []
  responses:
    '200':
      description: Build job projection page.
      content:
        application/json:
          schema: { $ref: ../components/schemas/enveloped-build-job-list.yaml }
    '401': { $ref: ../components/responses/unauthorized.yaml }
    '403': { $ref: ../components/responses/forbidden.yaml }
    '500': { $ref: ../components/responses/internal-server-error.yaml }

post:
  tags: [build]
  operationId: postBuildJob
  summary: Submit a Dockerfile build Task for an authorized Application
  parameters:
    - name: Idempotency-Key
      in: header
      required: true
      schema:
        type: string
        minLength: 1
        maxLength: 128
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../components/schemas/build-job-create-request.yaml
  responses:
    '202':
      description: Dockerfile build Task accepted.
      content:
        application/json:
          schema:
            $ref: ../components/schemas/enveloped-task-receipt.yaml
    '400': { description: Invalid build request }
    '401': { $ref: ../components/responses/unauthorized.yaml }
    '403': { $ref: ../components/responses/forbidden.yaml }
    '409': { description: Idempotency-Key was previously used with different build input. }
    '500': { $ref: ../components/responses/internal-server-error.yaml }

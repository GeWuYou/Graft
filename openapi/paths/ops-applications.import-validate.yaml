post:
  tags: [application]
  summary: Validate a Compose application import
  operationId: postApplicationImportValidate
  x-graft-mcp:
    risk:
      level: low
      reason: Validates import inputs and static parsing without registering an application.
      reversible: true
      impact: Does not change applications, files, or runtime state.
    confirmation:
      required: false
      strategy: none
      ttl: PT0S
  description: Validates Compose import inputs, file discovery, and static parsing without persisting a application record.
  parameters:
    - $ref: ../components/parameters/locale-header.yaml
    - $ref: ../components/parameters/request-id-header.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../components/schemas/application-import-validate-request.yaml
  security:
    - bearerAuth: []
  responses:
    '200':
      description: Import validation result.
      headers:
        X-Request-Id:
          $ref: ../components/headers/request-id.yaml
      content:
        application/json:
          schema:
            $ref: ../components/schemas/enveloped-application-import-validate-response.yaml
    '400':
      description: Invalid import validation request.
      headers:
        X-Request-Id:
          $ref: ../components/headers/request-id.yaml
      content:
        application/json:
          schema:
            $ref: ../components/schemas/error-response.yaml
    '401':
      $ref: ../components/responses/unauthorized.yaml
    '403':
      $ref: ../components/responses/forbidden.yaml
    '409':
      description: Import conflicts with an existing registered application.
      headers:
        X-Request-Id:
          $ref: ../components/headers/request-id.yaml
      content:
        application/json:
          schema:
            $ref: ../components/schemas/error-response.yaml
    '500':
      $ref: ../components/responses/internal-server-error.yaml

get:
  tags: [tasks]
  summary: Read Task detail
  operationId: getTask
  x-graft-mcp:
    risk: { level: low, reason: Reads an authorized existing Task, reversible: true, impact: Does not change Task state. }
    confirmation: { required: false, strategy: none, ttl: PT0S }
  parameters:
    - $ref: ../components/parameters/locale-header.yaml
    - $ref: ../components/parameters/request-id-header.yaml
    - in: path
      name: taskId
      required: true
      schema: { type: integer, format: int64, minimum: 1 }
  security:
    - bearerAuth: []
  responses:
    '200':
      description: Task detail with server-authoritative capabilities and Stage timeline.
      headers:
        X-Request-Id: { $ref: ../components/headers/request-id.yaml }
      content:
        application/json:
          schema: { $ref: ../components/schemas/enveloped-task-detail.yaml }
    '401': { $ref: ../components/responses/unauthorized.yaml }
    '403': { $ref: ../components/responses/forbidden.yaml }
    '404':
      description: Task was not found or is outside the caller resource scope.
      headers:
        X-Request-Id: { $ref: ../components/headers/request-id.yaml }
      content:
        application/json:
          schema: { $ref: ../components/schemas/error-response.yaml }
    '500': { $ref: ../components/responses/internal-server-error.yaml }

post:
  tags: [application]
  summary: Validate a runtime template application source
  operationId: postApplicationCreateTemplateValidate
  description: Validates the selected Application Root template directory and eventual managed-root target without writing it.
  parameters:
    - $ref: ../components/parameters/locale-header.yaml
    - $ref: ../components/parameters/request-id-header.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../components/schemas/application-template-create-request.yaml
  security:
    - bearerAuth: []
  responses:
    '200':
      description: Template source validation result.
      content:
        application/json:
          schema:
            $ref: ../components/schemas/enveloped-application-create-validate-response.yaml
    '400':
      description: Invalid template create validation request.
      headers:
        X-Request-Id:
          $ref: ../components/headers/request-id.yaml
      content:
        application/json:
          schema:
            $ref: ../components/schemas/error-response.yaml
    '401':
      $ref: ../components/responses/unauthorized.yaml
    '403':
      $ref: ../components/responses/forbidden.yaml

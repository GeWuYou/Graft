get:
  tags: [build]
  operationId: getBuildWorkspaces
  summary: List Build Workspaces
  parameters:
    - $ref: ../components/parameters/locale-header.yaml
    - $ref: ../components/parameters/request-id-header.yaml
    - name: search
      in: query
      schema: { type: string, maxLength: 255 }
    - name: limit
      in: query
      schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
    - name: offset
      in: query
      schema: { type: integer, minimum: 0, default: 0 }
  security: [{ bearerAuth: [] }]
  responses:
    '200':
      description: Caller-scoped Build Workspace list.
      content:
        application/json:
          schema: { $ref: ../components/schemas/enveloped-build-workspace-list.yaml }
    '400': { $ref: ../components/responses/bad-request.yaml }
    '401': { $ref: ../components/responses/unauthorized.yaml }
    '403': { $ref: ../components/responses/forbidden.yaml }
    '500': { $ref: ../components/responses/internal-server-error.yaml }
